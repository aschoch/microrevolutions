<%= form_for(current_user.participations.find_by_project_id(@project),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit t("buttons.unjoin"), class: "btn btn-large" %>
<% end %>